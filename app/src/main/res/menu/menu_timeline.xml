<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.photoviewer.photoviewer.TimelineActivity">
    <item android:id="@+id/toExplore"
        android:title="Explore"
        android:onClick="onExploreView"
        android:icon="@drawable/ic_search_action"
        android:showAsAction="ifRoom" />
    <item android:id="@+id/toProfile"
        android:title="Profile"
        android:icon="@drawable/ic_profile"
        android:onClick="onProfileView"
        android:showAsAction="ifRoom" />
</menu>
